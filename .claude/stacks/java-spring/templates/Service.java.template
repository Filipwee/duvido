package com.example.app.MODULO;

import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.util.UUID;

/**
 * Contrato do serviço de NOME.
 * Implementação: NomeServiceImpl
 */
public interface NomeService {

    NomeResponse create(NomeRequest request);

    NomeResponse findById(UUID id);

    Page<NomeResponse> list(Pageable pageable);

    NomeResponse update(UUID id, NomeRequest request);

    void delete(UUID id);
}
